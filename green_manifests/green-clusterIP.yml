apiVersion: "v1"
kind: "Service"
metadata:
  name: "green-clusterip"
  namespace: "default"
  labels:
    app: green
    job: green-app
spec:
  ports:
  - name: http
    protocol: "TCP"
    port: 80
    targetPort: 8080
  selector:
    app: "green"
  type: "ClusterIP"